# دليل نظام سجلات سير العمل (Workflow Logs System)

## نظرة عامة

يوفر هذا النظام "قصة" كاملة لكل دورة بحث عن صفقات يقوم بها المحرك. بدلاً من مجرد عرض النتائج النهائية، يمكنك الآن تتبع الخطوات المنطقية التي اتخذها المحرك، الشروط التي تم فحصها، والنتائج التفصيلية لكل مكون من مكونات الاستراتيجية.

## كيف تقرأ "قصة البحث" في السجلات؟

كل عملية بحث عن صفقات (Scan Cycle) تتبع تسلسلاً زمنياً منطقياً يظهر في السجلات كالتالي:

### 1. بداية الرحلة (`analysis_start`)
تظهر عند استلام شمعة جديدة وبدء عملية البحث.
*   **المحتوى:** العملة، الإطار الزمني المحفز، ووقت البداية.

### 2. خطوات التحليل (`analysis_step`)
هذه هي الخطوات التفصيلية التي توضح "ماذا يحدث الآن". لكل خطوة حالة (`success` أو `failed`) ورسالة توضيحية.

**أمثلة على الخطوات:**
*   **timeframe_setup:** تحديد الأطر الزمنية التي سيتم فحصها (مثلاً: 15m, 1h, 4h).
*   **analysis_[timeframe]:** بدء وانهاء تحليل إطار زمني محدد.
*   **component_[strategy]:** نتائج تحليل الاستراتيجيات الفرعية (SMC, Trend, Momentum, Volume).
*   **htf_filter:** نتيجة مطابقة الاتجاه مع الإطار الزمني الأعلى.
*   **structure_check:** نتيجة فحص بنية السوق (BOS/CHOCH).
*   **regime_check:** فحص حالة السوق (هل هو متذبذب جداً؟).
*   **confidence_gate:** هل درجة الثقة كافية لفتح صفقة؟
*   **risk_assessment:** تقييم المخاطر وحساب حجم الصفقة.

### 3. القرار النهائي (`decision_approved` أو `decision_rejected`)
خاتمة القصة، توضح هل تم قبول الصفقة أم رفضها مع ذكر السبب النهائي.

---

## مثال عملي لـ "قصة بحث" فاشلة (بسبب حالة السوق)

في واجهة Render، ستشاهد التسلسل التالي:

1.  `analysis_start`: "بدء دورة البحث عن صفقات لعملة BTCUSDT..."
2.  `analysis_step`: "تم تحديد الأطر الزمنية: LTF=15m, HTF=4h"
3.  `analysis_step`: "تم تحليل بنية السوق (15m): الاتجاه=up"
4.  `analysis_step`: "تم تحليل SMC (15m): تم العثور على 2 مناطق OB..."
5.  `analysis_step`: "فحص حالة السوق: VOLATILE (مرفوض - تذبذب عالي)"
6.  `decision_rejected`: "النتيجة: رفض الصفقة. السبب: regime_check_failed"

---

## المكونات التقنية

### 1. Workflow Logger (`monitoring/workflow_logger.py`)
الوحدة المسؤولة عن تنسيق الرسائل باللغة العربية والإنجليزية لضمان وضوح السجلات.

### 2. Tracing in Orchestrator (`engine/orchestrator.py`)
تم دمج نقاط التتبع مباشرة داخل منطق اتخاذ القرار لضمان دقة المعلومات المسجلة.

---

## فوائد هذا النظام
*   **الشفافية المطلقة:** تعرف بالضبط لماذا لم يفتح البوت صفقة في لحظة معينة.
*   **سهولة التصحيح (Debugging):** إذا كانت الاستراتيجية تعطي نتائج غير متوقعة، يمكنك رؤية أي خطوة هي التي تسببت في ذلك.
*   **تحسين الاستراتيجية:** من خلال مراقبة "أكثر الخطوات فشلاً"، يمكنك تعديل المعاملات (Thresholds) لتحسين الأداء.

---

## مراجع سريعة للأحداث

| الحدث | المعنى |
|-------|--------|
| `analysis_start` | "بدأت الآن" |
| `analysis_step` | "أقوم بفحص [الشرط] الآن" |
| `decision_approved` | "وجدت صفقة رائعة!" |
| `decision_rejected` | "لم تكتمل الشروط" |
| `trade_opened` | "فتحت الصفقة في السوق" |
| `trade_closed` | "انتهت الصفقة، وهذه هي النتيجة" |
