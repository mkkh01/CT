*** Begin Patch
*** Update File: Core/ai_engine.py
@@
-        diag_logger.smart_money_phase(analysis["smc_data"])
+        smc_data = self._normalize_smc_payload(analysis.get("smc_data", {}))
+        diag_logger.smart_money_phase(smc_data)
@@
 class AIEngine:
@@
     async def _handle_binance_error(self, e, attempt):
         error_str = str(e)
@@
         rate_limiter.set_ban(retry_after)
         diag_logger.system(f"BANNED: Binance 418/429 detected", retry_after=retry_after)
         raise Exception(f"BANNED: Binance 418/429 detected.")
+
+    def _normalize_smc_payload(self, smc_data):
+        """Guarantee a logger-safe SMC payload.
+
+        The production crash was caused by non-dict items reaching
+        DiagnosticLogger.smart_money_phase(). This wrapper preserves the
+        original structure when valid and coerces legacy/invalid payloads
+        into the expected dict shape.
+        """
+        if not isinstance(smc_data, dict):
+            return {
+                "SMC": {
+                    "exists": False,
+                    "info": f"Invalid SMC payload: {type(smc_data).__name__}",
+                    "confidence": 0,
+                }
+            }
+
+        normalized = {}
+        for key, details in smc_data.items():
+            if isinstance(details, dict):
+                normalized[key] = details
+            else:
+                normalized[key] = {
+                    "exists": bool(details),
+                    "info": str(details),
+                    "confidence": 100 if details else 0,
+                }
+        return normalized
*** End Patch
